# References for Register User Functionality

## User Model

- **Location:** `backend/users/models.py`
- **Relevance:** UUID pk, email as USERNAME_FIELD, `set_password()` for hashing, `full_clean()` enforces email uniqueness via `unique=True`

## UserFactory

- **Location:** `backend/users/tests/factories.py`
- **Relevance:** Pattern to follow for any factory needed in SIM-29

## JWT Token URL (existing)

- **Location:** `backend/core/urls.py`
- **Relevance:** `/api/auth/token/` uses `TokenObtainPairView` — register endpoint returns same `{ access, refresh }` shape using `RefreshToken.for_user(user)` from `rest_framework_simplejwt.tokens`

## SimpleJWT Config

- **Location:** `backend/core/settings/base.py`
- **Relevance:** `ROTATE_REFRESH_TOKENS = True`, `ACCESS_TOKEN_LIFETIME = 15min`, `REFRESH_TOKEN_LIFETIME = 7days` — these apply to tokens generated by the register endpoint too
