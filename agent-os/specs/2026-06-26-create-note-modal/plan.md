# SIM-50 — Create Note Modal

Full-page overlay modal opened by "+ New Note" button. Title, content, category select (required). Submits to POST /api/notes/, invalidates notes cache, closes on success.
