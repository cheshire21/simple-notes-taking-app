# Test List Categories — Shaping Notes

## Scope
Full test coverage for `category_list` selector. View GET tests were already written during SIM-40.

## Decisions
- Only `test_selectors.py` needs to be created
- 4 cases: own categories, exclude others, ordered by name, empty list
- No visuals needed
