# Plan: SIM-34 — Logout (FE)

## Context

Logout button for the dashboard. `logout()` API and `useLogout` hook already exist. Just needs the `LogoutButton` component and tests.

## Files to create

| File | Notes |
|------|-------|
| `features/auth/components/LogoutButton.tsx` | Client component |
| `features/auth/hooks/useLogout.test.tsx` | Hook tests |
| `features/auth/components/LogoutButton.test.tsx` | Component tests |

## Tasks

1. Save spec docs ✓
2. PM → SIM-34 In Progress ✓
3. nextjs-engineer implements
4. QA verifies done criteria
5. PM → Done (only if QA READY)
